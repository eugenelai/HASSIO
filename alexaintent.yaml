LocateIntent:
  speech:
    type: ssml
    text: >
      <speak>
      {%- if User.lower() == "eugene" -%}
      {{User}}'s current location is set to {{ states('device_tracker.iphone') }}.
      {%- else -%}
      Sorry. {{User}} isn't being tracked by me.
      {% endif %}
      </speak>

AvaSkillset:
  action:
    service: homeassistant.turn_on
    entity_id: script.aliceskills

UpdateMe:
  action:
    service: homeassistant.turn_on
    entity_id: script.systemupdate

amzn1.ask.skill.6f33c692-2547-41ce-af84-caf174917cf5:
  action:
    service: homeassistant.turn_on
    entity_id: script.systemupdate
